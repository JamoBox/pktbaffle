[package]
name = "pktbaffle"
version = "0.2.0"
edition = "2021"
description = "Compile libpcap-style packet filter expressions to classic BPF programs"
license = "MIT"
keywords = ["bpf", "pcap", "network", "packet", "filter"]
categories = ["network-programming", "parser-implementations"]
repository = "https://github.com/JamoBox/pktbaffle"
documentation = "https://docs.rs/pktbaffle"
readme = "README.md"
exclude = ["docs/"]

[features]
vm = []

[dependencies]

[dev-dependencies]
# Used by the live BPF socket test (Linux only). The whole workspace shares a
# single libc version, and pkttap requires >= 0.2.140 (for `libc::ifreq`), so
# this floor must match to avoid a conflict under `-Z direct-minimal-versions`.
libc = "0.2.140"

[[example]]
name = "dump_filter"
path = "examples/dump_filter.rs"

[[example]]
name = "bpf_live_test"
path = "examples/bpf_live_test.rs"
